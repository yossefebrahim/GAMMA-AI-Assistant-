org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# Must stay false: the Flutter gradle plugin (verified on Flutter 3.44.1) still reads the
# legacy AGP DSL types (BaseExtension) and fails to apply with an NPE under newDsl=true.
# Flip to true (the AGP 9 default; opt-out is removed in AGP 10) once the upstream
# migration lands: https://github.com/flutter/flutter/issues/180137
android.newDsl=false
